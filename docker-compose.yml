services:
  web:
    build: .
    ports:
      - "5000:5000"
    environment:
      - SECRET_KEY=${SECRET_KEY:-change-me-in-production}
      - DATABASE_URL=sqlite:///cooking.db
    volumes:
      - cooking_data:/app/instance
    restart: unless-stopped

volumes:
  cooking_data:
